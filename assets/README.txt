Place the VOLTEDGE logo here named exactly:  logo.png

Supported names (first match wins): logo.png, logo.jpg, logo.jpeg, logo.webp, logo.svg

The app auto-loads it for the sidebar and the login screen.
A transparent-background PNG looks best on the dark theme.
After adding/replacing the file, refresh the browser (the logo is cached;
if it doesn't update, restart the server).
